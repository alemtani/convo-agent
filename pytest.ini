[pytest]
asyncio_mode = auto
testpaths = tests
markers =
    live: hits real Claude/Azure APIs; needs keys and costs money. Run with `pytest -m live`.
# Default run is offline/mocked: exclude live tests. `pytest -m live` overrides this.
addopts = -m "not live"
