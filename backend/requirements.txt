fastapi==0.115.12
uvicorn==0.34.3
python-dotenv==1.1.0
anthropic==0.111.0
azure-cognitiveservices-speech==1.42.0
python-multipart==0.0.20
pydantic==2.11.3
aiosqlite==0.21.0
pypinyin==0.55.0
# A7: filing a learner's report as a GitHub issue. Already present as a test
# dependency (FastAPI's TestClient); promoted here because `backend/issues.py`
# calls out at runtime and a deploy that installs only this file needs it.
httpx==0.27.0

# Test suite
pytest==8.2.0
pytest-asyncio==0.23.7
pytest-cov==7.1.0
